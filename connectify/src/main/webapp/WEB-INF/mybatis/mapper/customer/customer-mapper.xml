<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="customerMapper">

    <select id="findById" parameterType="string" resultType="com.app.dto.customer.Customer">
        SELECT id, pw, name, email, phone
        FROM customer
        WHERE id = #{id}
    </select>

    <insert id="insert" parameterType="com.app.dto.customer.Customer">
        INSERT INTO customer (id, pw, name, email, phone)
        VALUES (#{id}, #{pw}, #{name}, #{email}, #{phone})
    </insert>

</mapper>